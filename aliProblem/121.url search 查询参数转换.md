> 编写一对函数实现 URL 查询参数和对象的互转。

```
/**
 * 把 URL 查询参数解析为一个对象
 * @param {String} search 查询参数。格式 ?a=1&b=2
 * @return {Object} 返回对象
 */ 
function search2obj(search) {
// your code
};

/**
 * 把对象 转换为 URL 查询参数
 * @param {Object} obj 对象
 * @return {String} search 查询参数。格式 ?a=1&b=2
 */ 
function obj2search(obj) {
// your code
};

// 控制台输出
console.log(search2obj('?a=1&b=test'))
{a:'1', b:'test'}
console.log(obj2search({a:'1', b:'test'}));
?a=1&b=test
```

#### js代码如下
```js
/**
 * 把 URL 查询参数解析为一个对象
 * @param {String} search 查询参数。格式 ?a=1&b=2
 * @return {Object} 返回对象
 */ 
function search2obj(search) {
// your code
};

/**
 * 把对象 转换为 URL 查询参数
 * @param {Object} obj 对象
 * @return {String} search 查询参数。格式 ?a=1&b=2
 */ 
function obj2search(obj) {
// your code
};

// 控制台输出
console.log(search2obj('?a=1&b=test'))
// {a:'1', b:'test'}
console.log(obj2search({a:'1', b:'test'}));
// ?a=1&b=test
```