> 给定两个数组，要求合并为一个，并去重排序

```
const arr1 = [1,9,3,6,12];
const arr2 = [0,2,10,3,12,12];

merge(arr1, arr2) 		// [0,1,2,3,6,9,10,12]
```

#### js代码如下
```js
import assert from 'power-assert@1.6.1/build/power-assert.js';

function merge(arr1, arr2) {
  // todo
}

/*
 * --- 测试部分无需更改 ---
 */
try {
  const arr1 = [1,9,3,6,12];
	const arr2 = [0,2,10,3,12,12];
  assert.deepEqual(merge(arr1, arr2), [0,1,2,3,6,9,10,12]);
  console.log('通过');
} catch (err) {
  console.error(err);
  console.log('不通过');
}
```