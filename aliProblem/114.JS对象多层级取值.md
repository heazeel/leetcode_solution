构造一个通用方法，实现取对象中任意层级的内容。


例如：
```
const obj = {
	a: {
  	b: {
    	c: 1,
    }
  }
}
```

要求：
```
get('obj.a.b.c')
```

注意，obj任意层级都可能为空，不能报错，例如：
```
// 此时 get('obj.a.b.c') 返回 undefined;
const obj = {
	a: 1,
}
```

#### js代码如下
```js

```