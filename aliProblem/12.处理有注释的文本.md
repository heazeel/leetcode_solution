```
写一个函数来处理有注释的文本，输出无注释的结果，同时去除空格
如输入为:
weex, rax # and react
flutter
native !hybrid app

输出应该是：
weex, rax
flutter
native

代码调用类似这样
const result = solution("weex, rax # and react\nflutter\nnative !hybrid app", ["#", "!"])
result = "weex, rax\nflutter\nnative"
```


#### js代码如下
```js
/**
写一个函数来处理有注释的文本，输出无注释的结果，同时去除空格
如输入为:
weex, rax # and react
flutter
native !hybrid app

输出应该是：
weex, rax
flutter
native

代码调用类似这样
const result = solution("weex, rax # and react\nflutter\nnative !hybrid app", ["#", "!"])
result = "weex, rax\nflutter\nnative"
**/

```