有n个模块都需要获取客户端的相关信息，发送了相同的异步请求，比较浪费资源。 期望只发送1个请求，后续的请求都复用第一个请求返回的结果数据。n个模块独立执行，他们的执行顺序并不相同。如果执行到发送请求的代码，如果之前已经发送了请求，那么该模块会停住等待请求返回并复用请求结果。 


请实现一个全局锁类Lock，并且支持全局缓存






#### js代码如下
```js

// 参考用例: 
// const lock = new Lock();

// 异步获取数据任务，耗时2s，已mock返回
// const getInfoOrReturnCache = async ()=>{
//  console.log("获取异步数据")
// 	return {
//   	system:"IOS"
//   }
// }


// 模块执行代码
// async function doTask (taskNumber){
// 	 console.log("task:" + taskNumber)
// 	 await lock.wait();
// 	 const res = await getInfoOrReturnCache()
//   lock.notify();
//   console.log(res.system)
// }


// 模块调用
// doTask(1)
// doTask(2)
// doTask(3)
// doTask(4)


// 输出:
// task1
// 获取异步数据
// ... 等待 2000ms 
// IOS

// task2
// IOS
// task3
// IOS
// task4
// IOS
```