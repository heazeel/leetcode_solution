请见 Javascript Tab

#### js代码如下
```js
/*
 * 请输入值在 returnTrue 返回为 true。
 * 例如： console.log(returnTrue(X)) // 返回 true
 */
function returnTrue(x) {
    return (x < x) && (x === x) && (x > x)
}

// 以下输入答案
```