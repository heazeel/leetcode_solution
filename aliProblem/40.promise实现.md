> promise实现

```js
function MyPromise (fn) {
  // 你的代码
}


const myPromise = new MyPromise((resolve) => {
  setTimeout(() => { resolve('hellow world') }, 2000)
})
myPromise.then((res) => {
  console.log(res)
})
```

#### js代码如下
```js

```