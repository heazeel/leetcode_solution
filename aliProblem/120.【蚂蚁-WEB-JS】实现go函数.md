```
/**
 * 实现go函数，使得函数执行能输出如下结果规则
 * 
 *
 * @param ary
 */
 _.go(['a', 'b', 'c', 'd'], 2);
 // => [['a', 'b'], ['c', 'd']]

 _.go(['a', 'b', 'c', 'd'], 3);
 // => [['a', 'b', 'c'], ['d']]

 _.go(['a', 'b', 'c', 'd'], 5);
 // => [['a', 'b', 'c', 'd']]

 _.go(['a', 'b', 'c', 'd'], 0);
 // => []
 
 ```

#### js代码如下
```js

```